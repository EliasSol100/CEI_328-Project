services:

  # ── PHP + Apache ────────────────────────────────────────────────────────────
  app:
    build: .
    container_name: athina_app
    restart: unless-stopped
    ports:
      - "${APP_PORT:-80}:80"
    environment:
      DB_HOST:     db
      DB_USER:     ${DB_USER:-root}
      DB_PASSWORD: ${DB_PASSWORD:-}
      DB_NAME:     ${DB_NAME:-athina_eshop}
      DB_PORT:     3306
    volumes:
      # Uploads και color photos παραμένουν persistent (δεν χάνονται σε rebuild)
      - uploads_data:/var/www/html/uploads
      - color_photos:/var/www/html/assets/product_color_photos
      - yarn_colors:/var/www/html/assets/yarn_colors
    depends_on:
      db:
        condition: service_healthy

  # ── MariaDB ─────────────────────────────────────────────────────────────────
  db:
    image: mariadb:10.11
    container_name: athina_db
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: ${DB_PASSWORD:-}
      MARIADB_DATABASE:      ${DB_NAME:-athina_eshop}
      MARIADB_ALLOW_EMPTY_ROOT_PASSWORD: "yes"
    volumes:
      # Persistent database storage
      - db_data:/var/lib/mysql
      # Αυτόματο import του schema κατά την πρώτη εκκίνηση
      - ./sql/athina_eshop.sql:/docker-entrypoint-initdb.d/01_schema.sql:ro
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # ── phpMyAdmin (προαιρετικό, για διαχείριση DB) ─────────────────────────────
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: athina_phpmyadmin
    restart: unless-stopped
    ports:
      - "${PMA_PORT:-8080}:80"
    environment:
      PMA_HOST:     db
      PMA_USER:     root
      PMA_PASSWORD: ${DB_PASSWORD:-}
    depends_on:
      - db

volumes:
  db_data:
  uploads_data:
  color_photos:
  yarn_colors:
